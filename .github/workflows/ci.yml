name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Julia
        uses: julia-actions/setup-julia@v1
        with:
          version: '1.12'

      - name: Install build tools (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc

      - name: Instantiate Julia environment
        run: julia --project=. -e "using Pkg; Pkg.instantiate()"

      - name: Add JuliaC
        run: julia --project=. -e 'using Pkg; Pkg.add("JuliaC")'

      - name: Run tests
        if: runner.os == 'Windows'
        run: julia --project=. -e "using Pkg; Pkg.test()"

      - name: Build with JuliaC
        env:
          JULIA_CPU_TARGET: generic
          JULIA_CC: "gcc -Wl,--no-as-needed -lm"
        run: julia --project=. --startup-file=no -e "using JuliaC; JuliaC.main(ARGS)" -- --output-exe intsect-${{ runner.os }} --trim=no ../Intsect.jl

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ runner.os }}
          path: build-${{ runner.os }}
