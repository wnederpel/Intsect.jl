# in chess the hash is made like this:
# take the bitboards of all the separate pieces
# for each bitboard, take random 64 bit number
# multiply each bitboard with a random number and xor the results.
# we can pretty much to the same ?